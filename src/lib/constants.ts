import { LeadStatus } from '@/types';

export const statusOptions: LeadStatus[] = ['new', 'contacted', 'demo_scheduled', 'negotiation', 'converted', 'lost'];

// Placeholder for staff options. Prefer fetching staff list from API where needed.
export const staffOptions: string[] = [];
